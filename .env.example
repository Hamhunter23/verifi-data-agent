# Google Gemini API Key (Required)
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"

# Optional: Specify a Gemini Model (Defaults to gemini-1.5-flash-latest in the code)
# GEMINI_MODEL_NAME="gemini-1.5-pro-latest"

# Optional: Specify a persistent mailbox key for the agent on Agentverse
# (Defaults to "default_verifiable_data_agent_mailbox" in the code if not set)
# VERIFIABLE_DATA_AGENT_MAILBOX_KEY="your_unique_mailbox_key_here" 